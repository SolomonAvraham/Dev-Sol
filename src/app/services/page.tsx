import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dev-Sol Services",
  description:
    "No page builders or WordPress - We offer 100% hand-coded websites with superior results starting at $75/mo.",
};

export default function Services() {
  return <div>Services</div>;
}
